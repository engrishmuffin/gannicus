opera()
{
	//tare the current frame so that "number of frames elapsed" can be kept track of
	loadOgg();
}
~opera(){}

void loadOgg(){}

void tick()
{
	
	listen(p1Score, p2Score);
	//for i in events
	//	event.tick();
}

void listen(operaScore, operaScore)
{
	//Take in an operaSignal.
	//Check if even
	//Play the appropriate operaEvent
}
